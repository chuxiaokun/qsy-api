# 复制为 deploy.env 后填写（勿提交 deploy.env）

# SSH：用户@服务器IP 或 域名
DEPLOY_HOST=root@43.108.21.17

# 服务器上项目目录（与宝塔「运行目录」一致）
DEPLOY_PATH=/www/wwwroot/qsy-api

# PM2 项目名称（与宝塔 PM2 里「项目名称」一致）
PM2_NAME=qsy-api

# 安装依赖：npm 或 pnpm（项目自带 package-lock.json，默认 npm 即可）
PACKAGE_MANAGER=npm
